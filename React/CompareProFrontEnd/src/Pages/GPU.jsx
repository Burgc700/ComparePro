//Imports for components and styling used for the page.
import "./Home.css"
import { CategoryProducts } from "../Components/CategoryProducts"

//Function that defines the gpu page.
export function CPU() {
    //Returns the full based based on the components added on the return.
    return (
        <>
            <CategoryProducts/>
        </>
    )
}